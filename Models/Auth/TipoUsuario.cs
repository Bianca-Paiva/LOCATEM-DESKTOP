namespace LOCATEM_DESKTOP.Models.Auth
{
    /// <summary>
    /// Mesma nomenclatura usada no React (CardOpcaoConta, cadastroSchema): 'locatario' | 'locador'.
    /// </summary>
    public enum TipoUsuario
    {
        Locatario,
        Locador
    }
}
